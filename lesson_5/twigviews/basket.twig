<h2>Корзина</h2>

{% for item in basket %}
    <hr>
    <div>
        <h3>{{ item.name }}</h3>
        <p>{{ item.description }}</p>
        <p>price: {{ item.price }} count: {{ item.count }}</p>
        <button>Купить</button>
    </div>
{% endfor %}
